import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx';

// data connect to Youtube
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
